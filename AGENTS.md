# AGENTS.md

Rust crate (edition 2024), built with Cargo. No dependencies yet.

## Commands

- `cargo run` — build and run the binary
- `cargo test` — run tests (none exist yet)
- `cargo clippy` — lint

## Notes

- Remote is `github.com/krey-yon/Linkex`; crate name is `tross` (repo is the GitHub shell, crate name is the app).
- Rust 1.85+ required for edition 2024 — macOS toolchain typically fine, but ensure `rustup toolchain` is current.
- No CI, no lockfile committed yet (`Cargo.lock` is absent from the repo).
